== stylesheet_include 'default/new'

== render_block :header, title: "New #{@model.class.clean_name}", color: :green do
  == link_to(:index, @model, 'Cancel', class: 'btn btn-outline-danger back-button')
br
== render_block(:form, model: @model)
